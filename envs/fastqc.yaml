# Environment for FastQC — pinned exactly to the upstream nf-core/methylseq 4.2.0
# module environment (modules/nf-core/fastqc/environment.yml).
name: fastqc
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::fastqc=0.12.1
